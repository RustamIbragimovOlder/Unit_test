public class LengthCircle {
    public double calculate(int i) {
        double length = 2 * Math.PI * i;
        // double length = Math.round(2 * Math.PI * i); // вариант с округлением

        return length;
    }
}
